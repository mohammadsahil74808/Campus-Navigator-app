<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ═══════════════════════════════════════════════════════════════
         REQUIRED PERMISSIONS
         All these were MISSING from the original manifest — they caused
         silent failures and potential crashes.
    ═══════════════════════════════════════════════════════════════════ -->

    <!-- Camera — required for AR view + OCR localization -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Location — required for Google Maps + GPS localization -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Internet — required for Google Maps tiles + Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Sensors — required for compass/gyro fusion -->
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />

    <!-- Vibration — optional, used for haptic navigation cues -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Wake lock — keeps screen on during navigation -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Camera feature declaration (required for Play Store filtering) -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.compass" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="false" />

    <application
        android:label="Campus Navigator"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:hardwareAccelerated="true"
        android:largeHeap="true">

        <!--
            SECURITY FIX: API key moved to local.properties / build config.
            DO NOT commit real API keys to source control.
            In production, use:
              1. local.properties → MAPS_API_KEY=your_key
              2. build.gradle: manifestPlaceholders["MAPS_API_KEY"] = ...
              3. Here: android:value="${MAPS_API_KEY}"
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

</manifest>
